#pragma once
/**
 * Embedded Neural Amp Model Registry (Firmware)
 *
 * This file redirects to the shared model registry in core.
 * Kept for backwards compatibility with existing firmware includes.
 */

#include "core/effects/embedded/model_registry.h"
