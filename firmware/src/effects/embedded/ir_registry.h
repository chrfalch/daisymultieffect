#pragma once
/**
 * Embedded Cabinet IR Registry (Firmware)
 *
 * This file redirects to the shared IR registry in core.
 * Kept for backwards compatibility with existing firmware includes.
 */

#include "core/effects/embedded/ir_registry.h"
